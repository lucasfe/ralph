# Project context for Ralph

Ralph injects this addendum into its base prompt at runtime. Keep it
short — describe the stack, available MCPs, sub-agents, and any
restrictions specific to this project.

## Stack

Describe the project stack in one or two lines (framework, language,
notable tooling).

## MCPs

List MCP servers available in `.mcp.json`, if any, and when Ralph
should use them.

## Sub-agents / slash commands

List `/project:*` commands Ralph can delegate to (frontend, backend,
QA, docs, etc.).

## Extra restrictions

List anything else Ralph must avoid in this repo (paths, files,
patterns).
